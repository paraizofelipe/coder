# Worktrees criadas manualmente durante o trabalho — uma por branch.
# Mantidas dentro do repositório, mas fora do versionamento para não poluir o git status.
.wt/

# Binário compilado a partir de `go build` na raiz. O nome coincide com o do
# módulo, então um build local sujaria o git status a cada iteração.
/coder
